# Terraform 변수 설정 예시 파일
# 이 파일을 복사하여 terraform.tfvars 생성 후 실제 값 입력
# 명령어: cp terraform.tfvars.example terraform.tfvars

# AWS 설정
aws_region  = "ap-northeast-2"  # 서울
aws_profile = "default"         # AWS CLI 프로파일

# 환경 설정
environment = "dev"

# EC2 설정
instance_type = "t2.micro"              # 프리티어
instance_name = "back-app-server"
key_name      = "your-key-name"         # ⚠️ 실제 키페어 이름으로 변경 필요

# 네트워크 설정
allowed_ssh_cidr = ["0.0.0.0/0"]        # ⚠️ 보안: 본인 IP로 변경 권장
# allowed_ssh_cidr = ["123.456.789.012/32"]  # 예시: 본인 공인 IP

# 애플리케이션 설정
app_port = 8080

# 데이터베이스 비밀번호
mysql_root_password = "your_strong_root_password"  # ⚠️ 변경 필요
mysql_password      = "your_strong_user_password"  # ⚠️ 변경 필요

# 주의사항:
# 1. key_name: AWS EC2 키페어 이름 (콘솔에서 미리 생성 필요)
# 2. 비밀번호: 강력한 비밀번호 사용 (대소문자+숫자+특수문자)
# 3. allowed_ssh_cidr: 보안을 위해 본인 IP만 허용 권장
# 4. 이 파일은 절대 Git에 커밋하지 말 것!
